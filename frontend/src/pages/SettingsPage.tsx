import PagePlaceholder from '@/components/PagePlaceholder';

export default function SettingsPage() {
  return (
    <PagePlaceholder
      title="Settings"
      description="Which categories are billable, and the overhead for each month."
      plannedIn="MD-13"
    />
  );
}
