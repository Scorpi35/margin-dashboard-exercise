# Refuse to install on a Node that cannot run the project.
#
# better-sqlite3 ships no prebuilt binary for Node 20, and binds its native module
# lazily — so without this an install succeeds, the server starts, and only
# requests that touch the database fail. Fail at the first step instead.
engine-strict=true
