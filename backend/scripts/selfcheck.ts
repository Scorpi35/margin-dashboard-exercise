/**
 * Enforces the reconciliation invariant: with overhead forced to zero,
 * company-wide total cost equals total salaries to the dirham
 * (AED 2,400,000.00 for 2025 — see docs/cost-model.md).
 *
 * Wired up here as part of the scaffold (MD-1); the calculation engine it
 * exercises lands in a later issue. It exits non-zero rather than reporting a
 * pass, because a self-check that always succeeds is worse than none at all.
 */
console.error('selfcheck: not implemented yet — the calculation engine lands in a later issue.');
process.exit(1);
